import styled from "styled-components";

export const MenuItemBox = styled.div`
  display: flex;
  align-items: center;
  gap: 0.9rem;

  & > p {
    font-family: Helvetica;
    font-size: 1.4rem;
    line-height: 2rem;
    color: var(--white);
  }
`;
